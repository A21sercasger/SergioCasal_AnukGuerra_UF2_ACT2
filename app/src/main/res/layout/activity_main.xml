<<<<<<< Updated upstream
<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.example.sergiocasal_anukguerra_uf2_act2.PianoView
        android:id="@+id/piano_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

=======
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">

    <!-- White keys -->
    <LinearLayout
        android:id="@+id/white_keys"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:weightSum="14">

        <!-- 14 white keys -->
        <Button
            android:id="@+id/key1"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:layout_weight="1"
            android:background="@android:color/white"
            android:text="C" />

        <!-- Repeat for 13 more white keys (C, D, E, F, G, A, B, etc.) -->
    </LinearLayout>

    <!-- Black keys (positioned above) -->
    <LinearLayout
        android:id="@+id/black_keys"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingTop="20dp"
        android:weightSum="10">

        <!-- 10 black keys (C#, D#, F#, G#, A#) -->
        <Button
            android:id="@+id/key_sharp1"
            android:layout_width="0dp"
            android:layout_height="120dp"
            android:layout_weight="1"
            android:background="@android:color/black"
            android:text="C#" />

        <!-- Skip where there's no black key (E-F, B-C) -->
    </LinearLayout>

>>>>>>> Stashed changes
</RelativeLayout>